# 开发日志

> 项目：医者（Godot）  
> 本日志自 2026-08-05 起记录重要功能调整、修复与待办事项。

## 2026-08-05

对应提交：

- [`9ae453e1`](https://github.com/lqc19891229/Doctor/commit/9ae453e14d5ffcd60d2ca79ed917d86ccc89882f)
- [`5c506523`](https://github.com/lqc19891229/Doctor/commit/5c506523cb4d64ac323e0cfa7bc06cf72d6ca8a5)
- [`8b7ef8e6`](https://github.com/lqc19891229/Doctor/commit/8b7ef8e638226acd20b9418db23b7d4f53628626)

### 数据导入与剧情配置

- 扩展 `DataTables/import_data.py`：
  - NPC 的 `Disease` 字段可填写疾病 ID 或疾病名称。
  - 剧情 `EntryId` 可填写条目 ID、数据 ID、中文名称或自定义标题。
  - 对不存在的疾病、无法匹配的图鉴条目和重名条目增加导入校验，避免静默生成错误资源。
- 更新世子 NPC 数据及 005 系列剧情资源：
  - 调整世子疾病配置。
  - 为 `005_01` 补充图鉴解锁条目。
  - 更新 `005_02`、`005_03` 的剧情背景。
  - 调整部分对话中非当前角色立绘的显示方式。

### 剧情流程修复

- 修正 `Main.gd` 中诊室场景路径的大小写，避免在大小写敏感环境中加载失败。
- 剧情不再于 `set_story()` 时提前标记为已播放，改为剧情真正结束后记录。
- 连续剧情切换时先登记上一段剧情，避免漏记剧情链中的已完成章节。
- Game Over 剧情完成状态会写入内存，但不覆盖玩家最后一个可读取存档。
- 剧情结束返回夜晚时，先完成白天状态切换，再保存一次完整状态；其他返回分支也只保存一次。

### 存档可靠性修复

- 存档改为临时文件写入，成功后再替换正式存档。
- 替换正式存档前保留备份，失败时尝试恢复旧档。
- 新游戏覆盖槽位时不再提前删除旧存档；新档写入失败会保留原存档。
- 自动存档统一检查返回值，并在失败时输出明确警告。
- `has_save()`、`load_game()`、`delete_save()`、`get_save_meta()` 会在中断事务恢复失败时停止后续操作。

### 验证状态

- 已完成提交差异、脚本接口和基础结构的静态检查。
- 当前检查环境没有 Godot 可执行文件，尚未完成引擎内运行测试。
- 仓库当前没有自动化检查流程。

### 已知低优先级问题

- 存档恢复失败时，槽位界面仍可能显示为“空存档”，提示与实际状态不完全一致。
- 新版 1 号槽存档与旧版 `save_game.json` 同时存在时，`delete_save(1)` 只删除新版文件；旧存档可能再次显示。
